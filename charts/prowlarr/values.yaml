image:
  repository: docker.io/linuxserver/prowlarr
  tag: latest
  pullPolicy: Always

port: 9696
puid: 1000
pgid: 1000
tz: America/Denver

# prowlarr is an indexer manager only - no media, so no data volume.
storage:
  configClaim: mediaserver-config
  dataClaim: mediaserver-data
  mountData: false

service:
  type: ClusterIP
  port: 9696

probe:
  path: /prowlarr/ping

initConfig:
  enabled: true

ingress:
  enabled: true
  className: traefik
  host: media.thereedfamily.rocks
  path: /prowlarr
  # Zitadel SSO (oauth2-proxy forwardAuth); /prowlarr/api bypasses it upstream where applicable
  ssoMiddlewares: true
  tls:
    enabled: true
    secretName: prowlarr-tls
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt

nodeSelector: {}
resources: {}
